<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.registraponto"
    android:installLocation="internalOnly"
    android:versionCode="1"
    android:versionName="@string/versao_nome">

    <application
    android:name="android.support.multidex.MultiDexApplication"
    android:allowBackup="true"
    android:icon="@mipmap/icon"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:largeHeap="true"
    android:theme="@style/tela_base">

        <activity
            android:name="com.registraponto.TelaPrincipal"
            android:screenOrientation="portrait"
            android:theme="@style/tela_base"
            android:label="Registros de Pontos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.registraponto.TelaLista"
            android:windowSoftInputMode="stateHidden"
            android:screenOrientation="portrait"
            android:label="Lista de Pontos"
            android:theme="@style/tela_base"/>

    </application>

</manifest>